using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestMoveArm : MonoBehaviour
{
    // Start is called before the first frame update
   //public Vector3 Move;
   // [SerializeReference]float speed = 1;
   // void Start()
   // {
        
   // }

   // // Update is called once per frame
   // void Update()
   // {

   //     transform.localEulerAngles = Move;
   //     Move = new Vector3(Input.mousePosition.x * speed, Input.mousePosition.y * speed, Input.mousePosition.z * speed);
   // }
}
